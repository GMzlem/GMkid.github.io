# 🚀 빠른 시작 가이드

## 프로젝트 개요

예슈아 사랑교회 웹사이트를 완전히 리팩토링하여 모던한 구조로 재구성했습니다.

## 🎯 주요 개선사항

### 1. **구조화된 폴더 시스템**
- CSS 파일을 기능별로 분리 (reset, style, navbar, slider, sections)
- JavaScript를 모듈별로 분리 (app, slider, popup)
- 리소스를 assets 폴더로 정리

### 2. **코드 품질 향상**
- 인라인 스타일 제거 → 외부 CSS 파일로 분리
- 반복되는 코드 제거 및 재사용 가능한 클래스 생성
- CSS 변수를 활용한 테마 관리

### 3. **반응형 디자인**
- 모바일 최적화 (768px, 480px 브레이크포인트)
- 터치 제스처 지원
- 모바일 메뉴 구현

### 4. **성능 최적화**
- 이벤트 위임 패턴 사용
- 디바운스/스로틀 유틸리티 함수
- 효율적인 DOM 조작

### 5. **사용자 경험 개선**
- 부드러운 스크롤 애니메이션
- 슬라이더 자동재생 + 수동 조작
- 키보드 접근성 향상

## 📦 파일 설명

### HTML 파일
- `index.html` - 메인 페이지 (교회소개, 예배안내, 소식, 위치)
- `sundayschool.html` - 주일학교 전용 페이지

### CSS 파일
- `reset.css` - 브라우저 기본 스타일 초기화
- `style.css` - 전역 스타일 및 공통 컴포넌트
- `navbar.css` - 네비게이션 바 스타일
- `slider.css` - 이미지 슬라이더 스타일
- `sections.css` - 각 섹션별 스타일
- `sundayschool.css` - 주일학교 페이지 전용 스타일

### JavaScript 파일
- `app.js` - 메인 애플리케이션 (네비게이션, 스크롤, 유틸리티)
- `slider.js` - 이미지 슬라이더 기능
- `popup.js` - 모달 팝업 기능

## 🎨 디자인 시스템

### 컬러 팔레트
```css
주 색상: #50a2e9 (파란색)
보조 색상: #a7a7a7 (회색)
텍스트(진): #323232
텍스트(연): #9e9e9e
배경: #f0f0f0
흰색: #ffffff
```

### 타이포그래피
- 헤더: 2.5rem ~ 4rem
- 본문: 1rem ~ 1.2rem
- 줄 간격: 1.6 ~ 2

### 간격
- 섹션 패딩: 50px ~ 100px
- 카드 패딩: 30px ~ 50px
- 요소 간격: 15px ~ 40px

## 🛠️ 로컬 테스트 방법

### ⚠️ 중요: 파일 경로 정보
모든 CSS, JavaScript, 이미지 경로를 **상대 경로**로 설정했습니다.
- ✅ HTML 파일을 직접 더블클릭해서 열어도 작동합니다
- ✅ 로컬 서버 없이도 테스트 가능합니다
- ✅ 웹 서버에 업로드해도 바로 작동합니다

### 방법 1: 파일 직접 열기 (가장 쉬움)
```
1. yeshua-church 폴더로 이동
2. index.html 파일을 더블클릭
3. 브라우저에서 바로 확인 가능!
```

### 방법 2: Python 서버 (추천)
```bash
cd yeshua-church
python -m http.server 8000
# 브라우저에서 http://localhost:8000 접속
```

### 방법 3: Node.js http-server
```bash
cd yeshua-church
npx http-server
# 표시되는 URL로 접속
```

### 방법 4: VS Code Live Server
1. VS Code 확장 프로그램 "Live Server" 설치
2. index.html 파일 우클릭
3. "Open with Live Server" 선택

## 📝 제거된 기능

원본 코드에서 다음 기능들을 제거했습니다:
- ✅ Band (밴드) 링크
- ✅ Instagram (인스타그램) 링크

## 🔄 원본과의 차이점

| 항목 | 원본 | 리팩토링 |
|------|------|----------|
| CSS | 인라인 스타일 | 외부 파일 분리 |
| JS | 전역 함수 | 모듈 패턴 |
| 구조 | 단일 파일 | 기능별 분리 |
| 반응형 | 부분 지원 | 완전 지원 |
| 접근성 | 기본 | 향상됨 |
| 유지보수성 | 낮음 | 높음 |

## 🚨 주의사항

### 이미지 경로
- 현재는 `/assets/images/` 경로를 사용
- 실제 서버에 업로드 시 이미지 파일들을 올바른 위치에 배치해야 함

### 외부 링크
- KAICAM 링크: `http://home.kaicam.org/index.asp`
- 카카오톡 오픈채팅: `https://open.kakao.com/o/scbvDSfg`
- 구글 지도: 임베드 코드 포함됨

## 📱 테스트 체크리스트

- [ ] 데스크톱 (1920px 이상)
- [ ] 노트북 (1366px ~ 1920px)
- [ ] 태블릿 (768px ~ 1024px)
- [ ] 모바일 (480px 이하)
- [ ] 슬라이더 자동재생 작동
- [ ] 슬라이더 수동 조작 (버튼, 인디케이터)
- [ ] 네비게이션 링크 작동
- [ ] 팝업 모달 열기/닫기
- [ ] 스크롤 투 탑 버튼
- [ ] 모바일 햄버거 메뉴

## 💡 향후 개선 제안

1. **이미지 최적화**
   - WebP 포맷 사용
   - 이미지 지연 로딩 (Lazy Loading)

2. **성능 향상**
   - CSS/JS 파일 압축
   - CDN 사용

3. **기능 추가**
   - 다크 모드
   - 검색 기능
   - 갤러리 페이지

4. **접근성 개선**
   - ARIA 레이블 추가
   - 포커스 관리
   - 스크린 리더 지원 강화

## 📧 지원

문제가 발생하거나 질문이 있으시면 README.md 파일의 문의처를 참고하세요.

---

**개발 완료일**: 2024
**버전**: 2.0.0 (완전 리팩토링)
